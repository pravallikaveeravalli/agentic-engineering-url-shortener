Task T082a — RunSubmissionController. Genuine red-phase capture, caught by the real POL-SEC-002 policy
check running against the real repository, not staged.

RunSubmissionControllerIT.java's own fixture planted a fake creator-credential literal
("Bearer crk_should-be-completely-ignored") to prove T082a's own Guard clause -- "a presented creator
credential is never read" -- by sending one and asserting it changes nothing. That literal is itself a
contiguous match for the secret-scan pattern crk_[A-Za-z0-9_-]{16,}, in test source under scope for
POL-SEC-002's real scan (the same class of self-inflicted false positive this project already hit once
before, during T110's ReleaseBlockingConditionsIT work).

Command:
  ./scripts/build.sh -q -Dtest=RunSubmissionControllerIT -DfailIfNoTests=false verify

Result: RunSubmissionControllerIT itself: 4/4 green. But the SAME failsafe run also re-evaluates
PolicySetEvaluatorIT against the real, currently-committed project state:

  [ERROR] PolicySetEvaluatorIT.realEvaluationProducesAVerdict:53 the real project, as committed, must not
  block: 12 checks evaluated under policy-set-1.1.0, 1 mandatory FAIL ([POL-SEC-002]), 0
  EXCEPTION_REQUESTED, blocking=true ==> expected: <false> but was: <true>

Confirmed directly:
  ./scripts/scan.sh
  scanning for committed secrets...
  src/test/java/agentic/shortener/orchestration/api/RunSubmissionControllerIT.java:87:
      headers.set("Authorization", "Bearer crk_should-be-completely-ignored");
    ^^ matched secret pattern: crk_[A-Za-z0-9_-]{16,}
  FAIL: secret pattern matched above. Nothing is committed until it is removed.

Fix: the literal was split into two concatenated string pieces ("crk_" + "should-be-completely-ignored")
so the contiguous pattern never appears in source; the runtime header value sent to the server is
byte-identical, so the test still proves exactly what it proved before.

Re-run:
  ./scripts/scan.sh
  scanning for committed secrets...
  no secret-pattern matches
