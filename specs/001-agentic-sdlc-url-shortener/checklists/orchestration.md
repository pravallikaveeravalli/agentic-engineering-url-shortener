# Orchestration, Autonomy, Reliability & Observability Checklist

**Purpose**: Detect linear chaining disguised as orchestration, and verify that autonomy, reliability, and audit
obligations are specified in falsifiable form
**Created**: 2026-09-20
**Feature**: [spec.md](../spec.md) · [plan.md](../plan.md) · **Constitution**: v1.1.0
**Domains**: 4 (Agentic orchestration), 5 (Controlled autonomy), 9 (Reliability & recovery), 10 (Observability & auditability)
**Mode**: **Artifact/evidence-assessing.** Several conditions the owner requires detected — missing persistent
state, missing synchronization, unlimited retries, invalid rollback claims — are properties of artifacts and
behavior, not of requirement prose, and cannot be expressed as requirements-quality questions.

**Note**: Generated by `/speckit-checklist`. IDs continue from `architecture.md`.

**Review Ownership**: Reviewer-owned. Items generated unchecked. `[x]` is the reviewer's judgment, not a
completion claim.

**Phase tags**: `[Now]` · `[Post-Tasks]` · `[Pre-Release]`

---

## Orchestration Is Not a Linear Chain

- [ ] CHK077 — Is the execution model an explicit dependency graph or equivalent stateful model, rather than a fixed sequence of calls? [Now] [Spec §FR-ORC-002, Constitution II]
- [ ] CHK078 — Are dependency edges **declared as data**, not inferred from call order in code? [Now] [Spec §FR-ORC-002, Missing Dependency Representation]
- [ ] CHK079 — Does at least one fan-out exist where independent stages execute in parallel, identified by name? [Now] [Spec §FR-ORC-003, Missing Synchronization]
- [ ] CHK080 — Does at least one synchronization point exist where a join waits on multiple branches, identified by name? [Now] [Spec §FR-ORC-003]
- [ ] CHK081 — Does at least one conditional branch exist whose not-taken path is a defined state rather than an absence? [Now] [Spec §FR-ORC-001, Coverage]
- [ ] CHK082 — Is a join specified to **block** while any required branch is incomplete or failed, rather than proceeding on partial success? [Now] [Spec §EC-018, Failure Path]
- [ ] CHK083 — Is the graph materialised **per run**, so a replanned run's topology can differ from the template and remain queryable? [Now] [ADR-008, Missing Dependency Representation]
- [ ] CHK084 — Is cycle rejection specified to occur **before** a replanned subgraph is committed? [Now] [Spec §EC-030, Failure Path]
- [ ] CHK085 — Does the demonstrated execution show genuine parallel overlap in its run history, not merely a graph that permits it? [Pre-Release] [Evidence, Linear Chaining]

## Persistent State & Decision Lineage

- [ ] CHK086 — Is workflow state specified to survive orchestrator-process termination? [Now] [Spec §FR-ORC-004, Missing Persistent State]
- [ ] CHK087 — Is workflow state specified to survive a **persistence-layer** restart, closing the in-memory-store loophole? [Now] [Spec §CL-009]
- [ ] CHK088 — Is state prohibited from existing only in process memory, including inside a store that is itself only in memory? [Now] [Failure Path, Spec §FR-ORC-004]
- [ ] CHK089 — Is every state transition recorded with from-state, to-state, actor, timestamp, and reason? [Now] [Spec §FR-ORC-023, Missing Decision Lineage]
- [ ] CHK090 — Are current state and transition history specified to be written atomically, so they cannot diverge? [Now] [ADR-008, Consistency]
- [ ] CHK091 — Is artifact provenance recorded — which stage produced which artifact, from which inputs, under which decisions? [Now] [Spec §FR-ORC-005, Missing Decision Lineage]
- [ ] CHK092 — Is every clarification, gate decision, replan event, and policy verdict persisted rather than held only in conversation? [Now] [Constitution §Repository as source of truth]
- [ ] CHK093 — Can a reviewer reconstruct a run from persisted records **without** the originating session? [Pre-Release] [Evidence, Spec §FR-ORC-023]

## Controlled Autonomy

- [ ] CHK094 — Are autonomy limits declared per stage rather than globally assumed? [Now] [Spec §FR-ORC-021]
- [ ] CHK095 — Is an action outside declared limits specified to be **refused and recorded**, not merely logged? [Now] [Failure Path, Spec §FR-ORC-021]
- [ ] CHK096 — Is the governed party structurally unable to grant itself authority — proposing but never ruling? [Now] [Spec §CL-006, Constitution III]
- [ ] CHK097 — Is a human-owned action specified as impossible for an agent at **any** autonomy setting, with no override path? [Now] [Failure Path, Constitution III]
- [ ] CHK098 — Is the executor kind recorded per execution, so who executed a stage is never inferred? [Now] [Spec §FR-ORC-029]
- [ ] CHK099 — Is a deterministic execution prohibited from being presented as AI work, and vice versa? [Now] [Spec §NFR-AUT-003, Fabricated Results]
- [ ] CHK100 — Is executor behavior prohibited from branching on recognition of specific demonstration inputs? [Now] [Spec §FR-ORC-028, CN-010]
- [ ] CHK101 — Is the system specified to process requirements outside the demonstration set without executor changes? [Now] [Spec §SC-016]
- [ ] CHK102 — Is governance scope (which approvals a replan voids) computed deterministically rather than by inference? [Now] [ADR-009, Failure Path]
- [ ] CHK103 — Does an out-of-scenario run exist in committed evidence, proving executors are generic? [Pre-Release] [Evidence, Spec §SC-016]

## Reliability: Retries, Timeouts, Fallback

- [ ] CHK104 — Is a retry bound declared numerically, with unbounded retry explicitly prohibited? [Now] [Spec §FR-ORC-014, Unlimited Retries]
- [ ] CHK105 — Are backoff and timeout behavior declared, not left to implementation discretion? [Now] [Spec §FR-ORC-014]
- [ ] CHK106 — Is failure classification specified as a **rule** a reader can apply, rather than an instruction to classify? [Now] [Measurability, Spec §CL-006]
- [ ] CHK107 — Is an unrecognized, unknown, or malformed failure specified to be treated as **permanent**, never defaulting to retryable? [Now] [Failure Path, Spec §EC-031]
- [ ] CHK108 — Is a timeout on a non-idempotent effect specified as **not** retryable, so exactly-once is not violated by replay? [Now] [Spec §EC-033, Consistency]
- [ ] CHK109 — Is exhaustion of the retry bound prohibited from being reported as success? [Now] [Failure Path, Spec §FR-ORC-014]
- [ ] CHK110 — Is fallback declared per stage that can degrade, and is fallback activation recorded **as fallback** rather than as primary success? [Now] [Spec §FR-ORC-015, Fabricated Results]
- [ ] CHK111 — Is a failing fallback specified to escalate rather than loop? [Now] [Failure Path, Spec §EC-023]

## Reliability: Rollback, Compensation, Safe-Stop, Resume

- [ ] CHK112 — Is every effect class assigned a reversibility classification, with a register a reader can consult? [Now] [Spec §Compensation Register, Invalid Rollback Claims]
- [ ] CHK113 — Is rollback prohibited on effects that land in an immutable store, so no rollback is claimed where none is possible? [Now] [Failure Path, Invalid Rollback Claims]
- [ ] CHK114 — Does every irreversible effect have a **named compensating action**, with no irreversible effect left without one? [Now] [Missing Compensation, Spec §FR-ORC-016]
- [ ] CHK115 — Is a stage declaring an irreversible effect without a compensating action specified to **fail to load**? [Now] [Failure Path, Spec §EC-034]
- [ ] CHK116 — Is an unclassified effect specified as compensate-only, never erased? [Now] [Failure Path, Spec §FR-ORC-016]
- [ ] CHK117 — Is double compensation for the same effect prohibited, including when a later retry succeeds? [Now] [Spec §EC-022, Consistency]
- [ ] CHK118 — Is safe-stop defined as a state with entry triggers enumerated, rather than as an aspiration? [Now] [Missing Safe-Stop, Spec §FR-ORC-017]
- [ ] CHK119 — Is safe-stop classified as non-terminal, with the terminal set enumerated exhaustively and disjointly? [Now] [Clarity, Spec §CL-005]
- [ ] CHK120 — Is resumption specified to reach a deterministic terminal outcome without duplicating committed effects? [Now] [Missing Resume Behavior, Spec §FR-ORC-018]
- [ ] CHK121 — Is concurrent resumption of one run prohibited by a stated mechanism? [Now] [Failure Path, Spec §EC-026]
- [ ] CHK122 — Is dynamic replanning specified to invalidate **only** affected downstream stages, leaving unaffected paths untouched? [Now] [Missing Dynamic Replanning, Spec §FR-ORC-019]
- [ ] CHK123 — Is an approval on an invalidated stage specified to be voided by a **superseding record**, never by deletion or edit? [Now] [Spec §EC-020, Constitution X]
- [ ] CHK124 — Are rollback, compensation, safe-stop, resumption, and replanning each demonstrated by an executed test whose outcome is recorded? [Pre-Release] [Evidence, Constitution VIII]

## Observability & Audit Evidence

- [ ] CHK125 — Is a correlation identifier specified to propagate to every log line, metric, trace span, and persisted row? [Now] [Spec §NFR-OBS-001]
- [ ] CHK126 — Are all six mandatory audit fields required on every audit record, with none optional? [Now] [Incomplete Audit Evidence, Constitution IX]
- [ ] CHK127 — Is audit immutability enforced by a **mechanism** (privilege denial) rather than by convention? [Now] [ADR-010, Measurability]
- [ ] CHK128 — Are audit corrections specified as appended records referencing the corrected one, never in-place edits? [Now] [Constitution X]
- [ ] CHK129 — Is the audit fail-closed rule **scoped**, so it applies where a human overseer depends on the trail and does not reach paths that write no audit rows? [Now] [Clarity, ADR-010]
- [ ] CHK130 — Is every record class that cannot carry the six mandatory fields classified as something other than audit? [Now] [Consistency, ADR-010]
- [ ] CHK131 — Is the MTTR formula stated, with all nine capture fields enumerated? [Now] [Measurability, Plan §7]
- [ ] CHK132 — Are unrecovered failures **excluded from the MTTR denominator** and reported separately as a count? [Now] [Plan §7, Fabricated Results]
- [ ] CHK133 — Is every MTTR exclusion **declared** rather than silently applied? [Now] [Constitution X, Plan §7]
- [ ] CHK134 — Is every quantitative figure required to state whether it was measured and under what conditions? [Now] [Constitution IX, Fabricated Results]
- [ ] CHK135 — Is a demonstration measurement prohibited from being presented as a production statistic? [Now] [Failure Path, Constitution IX]
- [ ] CHK136 — Is retention specified so that a purge cannot remove records needed to reconstruct a live, suspended, or freshly terminal run? [Now] [Spec §NFR-AUD-002, Incomplete Audit Evidence]
- [ ] CHK137 — Do the recorded runs contain retry, compensation, safe-stop, resumption, and replanning evidence that is **distinguishable from ordinary success**? [Pre-Release] [Evidence, Spec §US-5]
- [ ] CHK138 — Is every figure in the final reporting labelled measured or proposed, with zero unlabelled figures? [Pre-Release] [Evidence, Spec §SC-012]

## Notes

- `/speckit-implement` reads checklist state but does not modify markers.
- CHK085, CHK093, CHK103, CHK124, CHK137, CHK138 require executed runs and are not evaluable at generation.
